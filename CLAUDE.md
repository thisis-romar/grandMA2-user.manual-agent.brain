# CLAUDE.md

Project guidance for Claude Code. The canonical, cross-tool agent contract lives in
`AGENTS.md` (the open standard other agents also read). Claude Code does not read
`AGENTS.md` directly, so it is imported below — keep edits in `AGENTS.md`, not here.

@AGENTS.md
